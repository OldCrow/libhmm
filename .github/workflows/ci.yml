name: CI

on:
  push:
    branches: [ main ]
    paths-ignore: ['**.md', 'docs/**', 'LICENSE']
  pull_request:
    branches: [ main ]
    paths-ignore: ['**.md', 'docs/**', 'LICENSE']
  workflow_dispatch:
  # Monthly canary: catches drift with no code change to trigger it (runner
  # image rolls, toolchain updates) instead of discovering breakage at the
  # next real push.
  schedule:
    - cron: '15 3 1 * *'

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux / GCC / Release
            os: ubuntu-latest
            cc: gcc
            cxx: g++
            msvc: false

          # -Werror gated here only (not GCC, to keep one runner as a warnings
          # baseline without -Werror in case a future GCC-only warning needs
          # triage separately). Verified locally clean under AppleClang with
          # the same -Wall -Wextra -Wpedantic -Wpointer-arith set before
          # enabling; Clang/AppleClang share warning implementations closely.
          - name: Linux / Clang / Release
            os: ubuntu-latest
            cc: clang
            cxx: clang++
            msvc: false
            werror: true

          - name: macOS / AppleClang / Release
            os: macos-latest
            cc: clang
            cxx: clang++
            msvc: false

          - name: Windows / MSVC / Release
            os: windows-latest
            msvc: true

    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      with:
        persist-credentials: false
    # -------------------------------------------------------------------------
    # Toolchain setup
    # -------------------------------------------------------------------------
    - name: Activate MSVC toolchain (Windows)
      if: matrix.msvc
      uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0
      with:
        arch: x64

    - name: Set compiler environment (Unix)
      if: ${{ !matrix.msvc }}
      run: |
        echo "CC=${{ matrix.cc }}" >> $GITHUB_ENV
        echo "CXX=${{ matrix.cxx }}" >> $GITHUB_ENV

    # -------------------------------------------------------------------------
    # Configure
    # GTest is pulled via FetchContent if not found locally (see tests/CMakeLists.txt).
    # BUILD_SHARED_LIBS=OFF avoids DLL path issues on Windows.
    # -------------------------------------------------------------------------
    - name: Configure (Windows / MSVC)
      if: matrix.msvc
      shell: pwsh
      run: |
        cmake -B build `
          -G "Ninja" `
          -DCMAKE_C_COMPILER=cl `
          -DCMAKE_CXX_COMPILER=cl `
          -DBUILD_SHARED_LIBS=OFF `
          -DBUILD_EXAMPLES=OFF `
          -DBUILD_TOOLS=OFF `
          -DCMAKE_BUILD_TYPE=Release

    - name: Configure (Unix)
      if: ${{ !matrix.msvc }}
      run: |
        cmake -B build \
          -DCMAKE_BUILD_TYPE=Release \
          -DBUILD_SHARED_LIBS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -DBUILD_TOOLS=OFF \
          ${{ matrix.werror && '-DCMAKE_CXX_FLAGS=-Werror' || '' }}

    # -------------------------------------------------------------------------
    # Build
    # -------------------------------------------------------------------------
    - name: Build all targets
      run: cmake --build build --config Release --parallel

    # -------------------------------------------------------------------------
    # Test — 41/41 tests pass on all platforms
    # -------------------------------------------------------------------------
    - name: Test
      run: ctest --test-dir build -C Release --output-on-failure --timeout 60 -LE "known_broken|benchmark"

  # ---------------------------------------------------------------------------
  # ThreadSanitizer — catches data races (e.g. concurrent cache fills).
  # Linux/GCC only: TSan on macOS/AppleClang is unreliable with system libraries.
  # -O1 keeps stack frames intact for readable reports; BUILD_TESTS=ON required.
  # ---------------------------------------------------------------------------
  tsan:
    name: ThreadSanitizer (Linux / GCC)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      with:
        persist-credentials: false
    - name: Configure with TSan
      run: |
        cmake -B build-tsan \
          -DCMAKE_BUILD_TYPE=RelWithDebInfo \
          -DCMAKE_CXX_FLAGS="-fsanitize=thread -g -O1" \
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=thread" \
          -DBUILD_SHARED_LIBS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -DBUILD_TOOLS=OFF

    - name: Build
      run: cmake --build build-tsan --parallel

    - name: Test
      run: ctest --test-dir build-tsan -C RelWithDebInfo --output-on-failure --timeout 120 -LE "known_broken|benchmark"

  # ---------------------------------------------------------------------------
  # AddressSanitizer + UndefinedBehaviorSanitizer — catches memory errors and UB.
  # Linux/GCC only: ASan on macOS requires extra setup for system libraries.
  # -O1 -fno-omit-frame-pointer preserves stack frames for readable reports.
  # ---------------------------------------------------------------------------
  asan:
    name: ASan/UBSan (Linux / GCC)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      with:
        persist-credentials: false
    - name: Configure with ASan/UBSan
      run: |
        cmake -B build-asan \
          -DCMAKE_BUILD_TYPE=RelWithDebInfo \
          -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -g -O1 -fno-omit-frame-pointer" \
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined" \
          -DBUILD_SHARED_LIBS=OFF \
          -DBUILD_EXAMPLES=OFF \
          -DBUILD_TOOLS=OFF

    - name: Build
      run: cmake --build build-asan --parallel

    - name: Test
      env:
        ASAN_OPTIONS: detect_leaks=1:abort_on_error=1
        UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1
      run: ctest --test-dir build-asan -C RelWithDebInfo --output-on-failure --timeout 120 -LE "known_broken|benchmark"

  # ---------------------------------------------------------------------------
  # Quality jobs — ubuntu only; run independently of the build matrix.
  # pre-commit mirrors local hook policy. cppcheck remains a dedicated analysis step.
  # ---------------------------------------------------------------------------
  precommit:
    name: Pre-commit (ubuntu)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      with:
        persist-credentials: false
        fetch-depth: 0

    - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
      with:
        python-version: "3.x"

    - name: Cache pre-commit environments
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
      with:
        path: ~/.cache/pre-commit
        key: ${{ runner.os }}-pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}

    - name: Install pre-commit
      run: python -m pip install --upgrade pip pre-commit

    - name: Determine changed files
      id: changed-files
      shell: bash
      run: |
        if [[ "${{ github.event_name }}" == "pull_request" ]]; then
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
        else
          BASE_SHA="${{ github.event.before }}"
        fi
        HEAD_SHA="${{ github.sha }}"

        if [[ -z "$BASE_SHA" || "$BASE_SHA" == "0000000000000000000000000000000000000000" ]]; then
          BASE_SHA="$(git rev-parse HEAD~1)"
        fi

        mapfile -t changed < <(git diff --name-only --diff-filter=ACMR "$BASE_SHA" "$HEAD_SHA")
        if [[ ${#changed[@]} -eq 0 ]]; then
          echo "has_changes=false" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        {
          echo "has_changes=true"
          echo "files<<EOF"
          printf '%s\n' "${changed[@]}"
          echo "EOF"
        } >> "$GITHUB_OUTPUT"

    - name: Run pre-commit hooks on changed files
      if: steps.changed-files.outputs.has_changes == 'true'
      shell: bash
      env:
        CHANGED_FILES: ${{ steps.changed-files.outputs.files }}
      run: |
        mapfile -t files <<< "$CHANGED_FILES"
        pre-commit run --files "${files[@]}" --show-diff-on-failure --color=always

    - name: No changed files to check
      if: steps.changed-files.outputs.has_changes != 'true'
      run: echo "No changed files detected for pre-commit."

  lint:
    name: Cppcheck (ubuntu)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      with:
        persist-credentials: false
    - name: Install cppcheck
      run: |
        sudo apt-get update -qq
        sudo apt-get install -y --no-install-recommends cppcheck

    - name: cppcheck
      run: |
        cppcheck \
          --enable=warning,style,performance \
          --error-exitcode=1 \
          --suppress=missingIncludeSystem \
          --suppress=useStlAlgorithm \
          --suppress=shadowFunction \
          --suppress=virtualCallInConstructor \
          --suppress=constParameterReference \
          --suppress=noExplicitConstructor \
          --suppress=toomanyconfigs \
          --suppress=functionStatic \
          --std=c++20 \
          -I include \
          src/
